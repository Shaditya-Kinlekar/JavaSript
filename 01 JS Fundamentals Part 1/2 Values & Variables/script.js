let js = 'amazing';
console.log(40 + 8 + 23 - 10 + 9);

//**  Variables follow camelCase naming convention
let firstName = 'Shadz';
console.log(firstName);
console.log(firstName);

// ** should follow JS standards (convention)
let lastName = 'Kinlekar';
let firstNamePerson;

//  can be used
let age20;
let $onlySpecialAllowed = 3452;
let first_name_persons;
let $fuction;

// ! cannot be used
// let 3yes;
//  (cannot start with number)

// cannot use keywords for naming variables
// eg: let function;

//  **NOTE**
let PI = 3.1415;
// (all capital is used for constant(like reserved) variables whose values won't change)

// * variables should be descriptive
let myFirstJob = 'Programmer';
let myCurrentJob = 'Teacher';
// is way better than this ⬇️
let job1 = 'Programmer';
let job2 = 'Teacher';

console.log(myFirstJob);
